export interface LocationItem {
  title: string;
  address: string;
  location: string;
  account?: any;
  maxAcceptDate: string;
  status: boolean;
  id: string;
  arrivalBufferTime: number;
  departureBufferTime: number;
  transferBufferTime: number;
}
